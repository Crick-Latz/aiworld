{
  "name": "story-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AIWorld world-compiler (WP-01): lore + seed -> WorldSpec / DirectorSecrets / CompileReport",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=23.4"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
